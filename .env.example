# Gmail API Configuration
# Get your credentials from: https://console.cloud.google.com/

# OAuth 2.0 Credentials (for credentials.json)
# Download credentials.json from Google Cloud Console and place in project root

# Check interval in seconds (default: 60 seconds = 1 minute)
CHECK_INTERVAL=60

# Optional: Specify which labels to check (comma-separated, e.g., "INBOX,UNREAD")
GMAIL_LABELS=INBOX

# Optional: Maximum number of messages to fetch per check
MAX_MESSAGES=10

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Timezone (default: Europe/Berlin)
# Examples: America/New_York, Asia/Tokyo, UTC
TZ=Europe/Berlin
